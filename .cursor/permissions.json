// Generated by ai-coding-playbook — do not edit; regenerate via scripts/generate_rules.py
// Cursor Auto-review adapter (first tool adapter for git-workflow pack permissions).
// autoRun only: do not set terminalAllowlist/mcpAllowlist here (those replace IDE allowlists).
{
  "autoRun": {
    "allow_instructions": [
      "Git workflow scripts under scripts/git-workflow/ (new_worktree, cleanup_worktree, check_release, release) are routine when the git-workflow pack is enabled.",
      "When installed by the optional git-workflow-automation pack, publish.sh and finish.sh are routine: push of the change branch via publish.sh or as the first remote step inside finish.sh, then merge into develop plus push of develop plus remote feature/fix branch delete via finish.sh.",
      "After project checks are green, merging a feature/* or fix/* branch into develop is an expected integration step and should run without approval prompts.",
      "After a successful merge into develop, cleanup_worktree.sh and removing the merged worktree/branch are routine follow-up steps."
    ],
    "block_instructions": [
      "Force-push to main or develop must always pause for approval.",
      "Merging into main outside scripts/git-workflow/release.sh must pause for approval.",
      "Push of develop or delete of a remote feature/* or fix/* branch outside scripts/git-workflow/finish.sh must pause for approval.",
      "Hard reset, amend of a pushed commit, or git with --no-verify / skipped hooks must pause for approval."
    ]
  }
}
