# playbook.yaml — Configuration locale du projet

mode: prototyping
project_status: in_progress

packs:
  - local-first
  - git-workflow
  - git-workflow-automation

# Exclusions durables (décommentez pour activer)
disable_rules: []

check_command: "python -m unittest discover -s tests -v"

project_blurb: |
  ai-coding-playbook: org rules library (`rules/core/`,
  `rules/postures/`, `rules/packs/`) and feed tool (`generate_rules.py`)
  that writes derived data (`KIMI.md`) into consuming projects. Edit the library + `rules/manifest.yaml`,
  then regenerate. No business data pipeline or local business package in this repository.

keep_files:
  - project-overlay.mdc #?
  - project-overlay.md
